apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  selector:
    matchLabels:
      app: myapp1
  template:
    metadata:
      labels:
        app: myapp1
    spec:
      containers:
      - name: myapp1
        image: mylandmarktech/maven-web-app
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: myapp1
spec:
  type: NodePort
  selector:
    app: myapp1
  ports:
  - port: 80
    targetPort: 8080 
    nodePort: 30300
